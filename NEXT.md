# 다음에 할 일

*2026-08-16 기준. 납품 구성별 화면을 전부 찍어보고, 안 담은 기능이 새어나오던 곳을
아홉 군데 막았습니다. 그 부류를 검사 도구가 잡도록 만들어뒀어요.*

## 지금 어디까지 왔나

`maribot` 코드를 가져와 **모듈로 골라 담을 수 있는 형태**로 바꾸는 중입니다.

- ✅ 코드만 복사 (유저 데이터·깃 히스토리는 안 가져옴)
- ✅ 서버 고유 ID를 코드에서 걷어내 `mari/guild.json` 으로 분리
- ✅ 모듈 레지스트리(`mari/modules.py`) + 동적 로딩. 담을 기능은 `guild.json`의 `modules`가 결정
- ✅ 코그 간 직접 import 제거 (14개 코그가 전부 독립)
- ✅ 서버 고유 기능을 `mari/parked/` 창고로
  (캠프·명단·프로필·견학권 / 고확 · 유배 · 타운가이드 · AI 페르소나 · 레벨 등급제)
- ✅ **이름 변수화 + 첫 기동 입력받기** — `/초기설정` 슬래시 명령, 값은 `settings.json`
- ✅ **모듈 소유 관계** — 어떤 모듈이 어떤 채널·역할·기능키·데이터파일을 데려오는지
  `modules.py` 한 곳에. 담지 않은 기능의 설정 명령과 빈 JSON이 따라오던 걸 막았어요
- ✅ **등급 사다리를 설정으로** — `guild.json`의 `ranks`. 안 적으면 "대장 / 멤버" 두 칸
- ✅ `cogs/core.py` → `cogs/ids.py` (클래스도 `MariCore` → `MariIds`). '핵심'처럼
  보이는 이름인데 실제 내용은 게임 아이디 등록부라 헷갈렸어요
- ✅ **도움말이 모듈 구성을 봅니다.** 예전엔 고정 문자열이라 shop+birthday만 담아
  납품하면 `/도움말`이 **없는 명령 37개**를 안내했어요. 이제 줄마다 모듈 태그를 달고
  걸러냅니다. 카테고리 **이름**도 구성을 따라가요 (상점을 안 담으면 '지갑').
  `check_help.py`가 조합 7개로 검사합니다 (전부 0개)
- ✅ **진단 명령도 모듈 구성을 봅니다.** 게임을 안 담으면 `/테스트 이벤트`가,
  주식을 안 담으면 `/테스트 종가게시미리보기`가 아예 등록되지 않아요
- ✅ **소유 표 자동 검사** — `check_modules.py`가 `modules.py`의 소유 표를 실제
  코드와 대조합니다. 등록을 깜빡한 데이터 파일·채널·역할·기능 키, 유령 상수,
  도움말 태그 오타를 잡아요
- ✅ **납품 문서** — [README.md](README.md) 에 세팅 순서·권한·체크리스트. 특히
  Privileged Intent 2개는 안 켜면 봇이 기동조차 못 하는데 어디에도 안 적혀 있었어요
- ✅ **돈 계층 검사** — `check_money.py`. 저장이 실패하거나 파일이 깨졌을 때
  돈이 조용히 사라지지 않는지 봅니다 (30건)
- ✅ **설정이 깨지면 기동을 막습니다** (2026-08-15 결정) — 파일이 **있는데** 못 읽으면
  봇을 안 켜고 알림 웹훅을 울려요. 그대로 켜면 주문 안 한 기능까지 전부 켜진 채로
  납품되고, 나중에 고칠 때 그 데이터가 통째로 빠지거든요. 파일이 **없을** 때는
  예전처럼 전부 켭니다 (새 서버니까)
- ✅ **옛 `guild.json`도 그대로 동작** — 봇이 돌고 있는 PC에서 `git pull`만 해도
  기능이 조용히 빠지지 않아요. 안 읽는 항목·오타도 기동 로그가 짚어줍니다
- ✅ **안 담은 기능이 화면에 새어나오던 것 (2026-08-16)** — 아래 '기능 누출' 절 참고.
  명령은 전부 제대로 걸러지는데 **글자만** 남던 부류라 검사가 전부 통과했었어요.
  이제 `check_help.py`가 낱말까지 대조합니다
- ✅ **기동 실패인데 종료 코드가 0이던 것 (2026-08-16)** — 토큰이 없거나 `ids.json`이
  깨졌을 때 그냥 `return`해서, systemd·도커가 "정상 종료"로 읽고 재시작도 실패 표시도
  안 했어요. 설정이 깨졌을 때만 `sys.exit(1)`이었는데 셋을 맞췄습니다 (`main.py`)

현재 상태: 모듈 14개, 슬래시 명령 34개. `python tools/check_modules.py` 로 확인 가능.

**코드에 남은 서버 고유 이름은 이제 없습니다.** 주석·독스트링까지 정리했고,
일부러 남긴 두 곳만 예외예요. (아래 '다음에 할 것 → 그 외' 참고)

---

## 🕳️ 안 담은 기능이 새어나오던 곳 (2026-08-16, 해결됨)

납품 구성 아홉 개로 **유저가 실제로 보게 될 화면을 전부 찍어서** 대조했습니다.
검사 도구 넷은 그때도 전부 통과였어요. 도구가 보는 건 **명령**인데, 새던 건 **글자**라서요.

고친 곳 (전부 "주문하지 않은 기능이 보인다"는 한 가지 문제입니다):

| 어디 | 뭐가 보였나 |
|---|---|
| `/설정 채널지정내역` | 안 담은 기능의 채널·역할이 **"❌ 미설정"으로 전부 나열**. 위키만 주문한 서버가 채널 13칸·역할 7칸을 보고 있었어요 (지금은 1칸·2칸) |
| `/지갑 전체:True` | 주식을 안 담아도 주식 칸이 붙고, 심지어 **"주식 시스템을 불러올 수 없어서 0으로 표시됐어요"** 라는 고장 안내까지 |
| `/지갑` | 상점이 없어도 "🎒 소지품 가방" 칸이 붙고 "아직 상점에서 구매한 물품이 없어요" |
| `/통계` | 주식 칸("상장 0종목")이 항상 붙고, AI를 안 담아도 채팅량 칸이 영영 "집계된 채팅이 없어요" |
| `/감사로그` | **명령 설명문**에 "경제/상점/아이디/역할/주식/생일" 나열 |
| `/테스트 데이터점검` | 아이디를 안 담아도 "아이디 중복: 0건 (`/아이디 중복정리`로 정리 가능)" — **없는 명령을 안내** |
| `/위키` | 설명문의 예시 항목이 "소개/**생일**/MBTI" |
| 도움말 4줄 | `/통계`·`/감사로그`·`/테스트 데이터점검`·`/지갑` 안내문의 기능 나열 |

제일 나쁜 건 `/지갑 전체`였어요. 주식을 주문한 적 없는 서버의 관리자에게 **봇이 고장 난
것처럼** 보입니다. 이제 그 안내는 "주식을 담았는데 코그가 안 올라온" 진짜 사고일 때만 떠요.

### 이제 도구가 잡습니다

`check_help.py`에 **낱말 검사**를 붙였어요. 조합마다 (1) 도움말 카테고리 **제목과 본문**,
(2) 실제로 등록되는 슬래시 명령 **설명문**을 훑어서, 안 담은 모듈의 낱말이 있으면 잡습니다.
표는 `tools/check_help.py`의 `LEAK_WORDS`예요.

낱말을 추가할 때 주의할 점 두 가지 (주석에도 적어뒀습니다):

- **기능 이름으로만 쓰이는 낱말**이어야 해요. "이벤트"는 `/초기설정`이 받는 이름 네 가지
  중 하나라, 미니게임을 안 담아도 정당하게 나옵니다. 그래서 "하이로우"를 씁니다.
- **이름(재화·봇·이벤트·서버)은 넣지 마세요.** 모듈 소유가 아니라 런타임 설정이에요.

실제로 이 검사가 `/위키` 설명문의 "생일"을 잡았습니다. 위키 항목은 자유 입력이라 생일을
적어도 되지만, 생일 모듈이 없는 서버의 **명령 목록에 그 글자가 뜨는 게** 문제예요.

---

## 🔀 두 PC로 갈렸던 일 (2026-08-14, 해결됨)

집 PC에서 커밋을 안 한 채 회사 PC에서도 작업이 이어져서 브랜치가 갈렸습니다.
양쪽이 **같은 목표를 각자 한 번씩** 진행했어요. 정면 머지하면 18개 파일이 충돌해서,
하나씩 대조해 쓸 것만 옮겨오고 `git merge -s ours`로 정리했습니다. (커밋 `b9b1eed`)

다행히 결과물이 상당 부분 수렴해 있었어요 — 양쪽 `mari_config.py`의 데이터 파일
상수가 25개로 완전히 같았고, 충돌 대부분은 같은 결론을 다르게 쓴 문장이었습니다.

저쪽에서 건져온 것: `tools/check_help.py`, `ranks` 등급 설정화, 창고 문서에 빠져
있던 데이터 파일 경로. 일부러 안 가져온 것은 아래 '다음에 할 것'에 적어뒀어요.

**교훈은 하나입니다 — PC를 옮기기 전에 커밋하고 푸시할 것.** 안 그러면 이번처럼
같은 일을 두 번 하고, 그 둘을 합치는 데 또 시간을 씁니다.

---

## 회사 PC — 그냥 `git pull` 하면 됩니다

`guild.json`은 `.gitignore` 대상이라 깃이 안 건드려요. 그래서 pull하면 코드는 새것,
설정은 옛것이 됩니다. **그 상태로도 동작하도록 코드가 옛 키를 받아줍니다.**
아래는 "언젠가 정리하면 좋은 것"이지 급한 게 아니에요. 기동 로그가 알려줍니다.

집 PC에서 그 상황을 그대로 재현해 로그인 직전까지 띄워봤습니다 — 모듈 12개 로드,
슬래시 명령 30개, 동기화 payload 생성 성공, 생일 멘션 역할도 옛 키에서 살아났어요.

⚠️ 다만 **봇을 켜는 순간 슬래시 명령이 실운영 서버에 동기화됩니다.** 아래 '2. 바뀐
명령어 이름'의 변경이 즉시 반영돼요. 되돌리기 어려우니 알고 켜세요.

### 1. `guild.json`의 역할 키 이름 변경 (자동 처리됨)

```jsonc
// 전
"broadcast_mention": 123456789
// 후
"birthday_mention": 123456789
```

`/고확`을 창고로 보내면서 이 역할의 남은 쓰임이 생일 알림뿐이라 이름을 바꿨어요.
(원본 서버에선 고확 멘션과 생일 멘션이 같은 '시민권' 역할이라 상수 하나를 공유했습니다)

옛 이름(`broadcast_mention`)이 있으면 **그 값을 그대로 씁니다.** 생일 알림 멘션이
조용히 빠지는 일은 없어요. 대신 기동 로그가 "이름을 바꿔주세요"라고 한 줄 남깁니다.
바꾸는 건 언제 해도 돼요. (`mari_config.py`의 `_ROLE_ALIASES`)

### 1-1. 안 읽는 항목도 기동 로그가 알려줍니다

창고로 간 기능이 쓰던 설정(`town_guide`, `onboarding`, `personas`, `camps`,
`levels`, `affiliations`, `camp_leader`, `camp_overseer`)이 남아 있으면
"지워도 됩니다"라고 알려줘요. 오타로 적은 키는 "코드가 읽지 않아요"라고 따로 짚어줍니다.

예전엔 둘 다 **조용히 무시**됐어요. 역할 ID를 정성껏 채워 넣고 "왜 안 되지?" 하는
상황이 제일 나빠서 알려주도록 했습니다.

### 1-2. 원본 서버의 등급 제도를 되살리려면 `ranks`를 적으세요

0~4레벨 등급 사다리는 코드에서 걷어냈고, 이제 `guild.json`에 적는 값입니다.
**안 적으면 "대장 / 멤버" 두 칸으로만 게시돼요.** 원본 서버처럼 등급을 쓰려면:

```jsonc
"ranks": [
  {"key": "lv4", "label": "4LEVEL", "role": 123456789, "color": "보라"},
  {"key": "lv3", "label": "3LEVEL", "role": 234567890, "color": "파랑"}
]
```

색은 빨강/보라/파랑/청록/노랑/초록/회색 중에 고르고, 생략하면 순서대로 물려줍니다.
맨 위 '대장' 칸은 여기가 아니라 `/설정 명단 대장`으로 지정해요. 어느 등급에도 안
걸린 사람은 맨 끝 '미분류' 칸에 모이니 아무도 명단에서 빠지지 않습니다.
자세한 설명은 `mari/guild.example.json`의 `_ranks설명`에 있어요.

같이 사라진 키(그냥 지우면 됩니다): `town_guide`, `onboarding` 섹션 전체, `personas` 섹션 전체.

### 2. 바뀐 명령어 이름

| 전 | 후 | 왜 |
|---|---|---|
| `/설정 레벨 대장` | `/설정 명단 대장` | 레벨 등급제를 창고로 보냄 |
| `/에바시설정` | `/이벤트설정` | ↓ |
| `/테스트 에바시` | `/테스트 이벤트` | ↓ |
| `/설정 관리자 에바시` | `/설정 관리자 이벤트` | ↓ |
| `/설정 채널 에바시` | `/설정 채널 이벤트` | ↓ |
| `/테스트 마리상태` | `/테스트 ai상태` | ↓ |
| `/마리기억` | `/기억` | ↓ |
| 우클릭 → 앱 → `에바 보내기` | `송금하기` | ↓ |

↓로 표시한 일곱은 전부 같은 이유예요. **명령어 '이름'에는 일부러 서버 고유 이름을 넣지 않았습니다.**
클라이언트가 입력한 글자가 명령 이름에 들어가면, 디스코드 규격에 안 맞는 글자 하나로
**동기화가 통째로 실패**해서 명령어가 전부 사라져요. (설명문에는 들어갑니다)

**저장된 값은 전부 그대로예요.** `settings.json`의 `roles.chief_role`,
`roles.evashi_admin`, `channels.evashi_announce` 는 옛 키 이름 그대로 두었으니
다시 지정할 필요 없습니다. 디스코드에 반영되는 데 최대 한 시간 걸리고, 급하면
`/테스트 명령어동기화`로 당길 수 있어요.

### 3. `settings.json`은 손대지 마세요

`channels.level_roster` 와 `level_roster_message_ids` 는 **일부러 옛 이름 그대로** 뒀습니다.
코드 쪽 이름만 `_refresh_id_roster`로 바꿨어요. 키를 바꾸면 업데이트하는 순간
지정해둔 채널을 잃어버리고 기존 명단 메세지를 못 찾아 통째로 다시 올립니다.

### 4. 이름을 그대로 쓰려면 아무것도 안 해도 됩니다

원본 서버는 `/초기설정`을 실행할 필요가 없어요. 입력 전 기본값이 예전 이름
("에바"·"마리"·"에바시"·"에바스타운") 그대로라, 지금까지와 똑같이 동작합니다.
기동 로그에 "이름이 아직 기본값"이라는 안내만 한 줄 찍혀요.

---

## 이름은 어떻게 동작하나

`mari/mari_names.py` 한 파일이 전부입니다. 코드는 이름을 모르고 여기서 받아 써요.

```python
from mari_names import currency, bot_name, event_name, server_name, josa

f"{amount:,} {currency()}"                        # "1,000 에바"
f"{currency()}{josa(currency(), '을를')} 보냈어요"  # "에바를" / "코인을"
```

받는 이름 네 가지:

| 키 | 무엇 | 기본값 |
|---|---|---|
| `currency` | 서버 재화 | 에바 |
| `bot` | 봇 이름 | 마리 |
| `event` | 선착순 이벤트 | 에바시 |
| `server` | 서버 이름 | 에바스타운 |

**값은 `settings.json`의 `names` 섹션에 삽니다.** `guild.json`(배포 설정)이 아니라
런타임 설정에 둔 이유는 봇이 스스로 쓰는 값이기 때문이에요. 관리자가 `/초기설정`
(서버 관리자 전용)을 실행하면 입력창이 뜨고, 그 자리에서 저장됩니다.

### 반드시 지킬 것 두 가지

**① 조사를 손으로 붙이지 마세요.** "에바"는 받침이 없어서 "에바를"이지만 "코인"은
"코인을"이에요. 이름 뒤에 오는 을/를·이/가·은/는·와/과·로/으로·아/야·(이)에요·(이)다는
전부 `josa()`를 거쳐야 합니다. 안 그러면 이름을 바꾸는 순간 "코인를 보냈어요"가 돼요.

**② 이름을 모듈/클래스 변수에 담아두지 마세요.** `CURRENCY = currency()` 같은 식으로
두면 봇이 켜질 때 값이 굳어서, `/초기설정`으로 바꿔도 그 문구만 옛 이름으로 남습니다.
실제로 `help.py`의 도움말 표와 `economy.py`의 송금 모달, `gpt.py`의 AI 페르소나가
이 함정에 걸려 있어서 전부 호출 시점 생성으로 바꿨어요.

### 언제 반영되나

| 무엇 | 언제 |
|---|---|
| 메세지·임베드 문구, 도움말, AI 페르소나 | **바로** |
| 슬래시 명령 **설명문** | 봇 재시작 + `/테스트 명령어동기화` |
| 슬래시 명령 **이름** | 바뀌지 않음 (일부러) |

명령 설명은 데코레이터라 import 시점에 한 번만 만들어져요. `/초기설정` 응답에도
이 내용을 안내하고 있습니다.

### 이름 길이 제한이 12자인 이유

이름이 슬래시 명령 **설명문**에 들어가는데 디스코드 한도가 100자예요. 넘으면 그 명령만
빠지는 게 아니라 **동기화 전체가 실패**합니다. 짧은 이름으로 테스트하면 절대 안 걸리는
사고라, `tools/check_modules.py`가 이름을 상한까지 늘린 상태로 한 번 더 자동 검사해요.
지금 여유는 80자 / 100자입니다. (`--max-names` 참고)

⚠️ 설명문에 이름을 **두 번 이상 넣지 마세요.** `/설정 채널 이벤트` 설명이 이름을 세 번
쓰다가 99자까지 갔었어요. 한 자만 더 길었으면 명령어가 통째로 사라졌습니다.

---

## 다음에 할 것

**급한 건 없습니다.** 아래는 전부 "언젠가" 항목이에요.

### 1. 원본 서버의 등급 제도를 되살릴지 (선택)

0~4레벨 사다리를 다시 쓰려면 회사 PC의 `guild.json`에 `ranks`를 적으면 됩니다.
위 '1-2' 참고. 안 적으면 "대장 / 멤버" 두 칸으로 게시돼요 — 그것도 정상 동작이라,
등급이 꼭 필요한 게 아니면 안 해도 됩니다. 역할 ID가 필요해서 그 PC에서만 가능해요.

### 2. 임베드 속 글자를 자동으로 검사할지 (선택, 아마 안 해도 됨)

지금 검사 도구가 못 보는 마지막 구멍이에요. 명령을 **실행해야** 나오는 글자라, 잡으려면
가짜 `Interaction`을 만들어 명령을 실제로 불러야 합니다. 디스코드 객체를 흉내 내는
비용이 커서, 얻는 것에 비해 품이 많이 들어요.

**대신 규칙 하나로 막는 편이 낫습니다** — 임베드에 칸을 붙일 때 그 칸이 특정 기능의
것이면 `module_active()`로 감싸세요. 위 '밟았던 함정'에 적어뒀어요. 지금 코드에서
`module_active`를 부르는 곳을 보면 패턴이 보입니다:

```bash
grep -rn "module_active" mari/cogs/
```

### 3. 그 외

- **🔐 `/역할부여`는 서버 관리자 전용으로 둡니다. 그대로 두세요.** 원래는 '타운가이드'
  역할도 쓸 수 있었는데, 그 역할을 창고로 보내면서 지정 가능한 역할이 안 남았어요.
  회사 PC에서는 이걸 되살리려고 `/설정 관리자 역할부여`(`set_role_admin`)를 실제로
  만들었는데, **합치면서 일부러 뺐습니다.** `/역할부여`는 아무 역할이나 지급할 수
  있어서, 위임받은 사람이 관리자급 역할을 스스로 챙기는 권한 상승 통로가 돼요.
  꼭 위임해야 한다면 "부여 가능한 역할 목록"을 함께 설정으로 받아서 **그 안에서만**
  고르게 막고, 관리자 권한이 딸린 역할은 걸러내야 합니다. 그 장치 없이는 열지 마세요.
- **주석의 옛 이름은 정리했습니다.** 일반 명사처럼 쓰던 것만 바꿨어요
  (에바→재화, 에바시→이벤트, 에바스타운→서버, 마리→봇). 내력 설명은 그대로 뒀습니다.
  일부러 남긴 두 곳 — `gpt.py`의 "고양이 두 **마리**"(단위 명사)와
  `mari_client.py`의 기본값 설명("에바"·"마리"…). 둘 다 바꾸면 안 돼요.

---

## 작업 요령

### 확인하는 법

```bash
python -m venv .venv
.venv\Scripts\python -m pip install -r mari/requirements.txt

.venv\Scripts\python tools/check_modules.py                 # 지금 설정 그대로
.venv\Scripts\python tools/check_modules.py shop birthday   # 이 조합만 주문받았다면
.venv\Scripts\python tools/check_modules.py --none          # 설정 없는 새 서버처럼
.venv\Scripts\python tools/check_modules.py --max-names     # 이름을 상한까지 늘렸을 때

.venv\Scripts\python tools/check_help.py                    # 도움말 ↔ 실제 명령 (조합 7개)
.venv\Scripts\python tools/check_help.py shop birthday      # 이 조합만
.venv\Scripts\python tools/check_guild.py                   # guild.json 읽기 (등급·명단 칸·옛 키)
.venv\Scripts\python tools/check_money.py                   # 돈 계층 (저장 실패·파일 손상)
```

전부 문제가 있으면 종료 코드 1로 끝나요. 기능을 빼거나 추가했다면 **네 개를 전부**
돌리세요. 서로 다른 걸 봅니다 — `check_modules`는 코그가 올라오고 동기화 규격이
맞는지(+ 소유 표가 코드와 일치하는지), `check_help`는 도움말이 유령 명령을
안내하는지 **그리고 안 담은 기능의 이름이 문구에 남았는지**, `check_guild`는
`guild.json`을 제대로 읽는지(등급·명단 칸·옛 키 이름), `check_money`는 저장이
실패하거나 파일이 깨졌을 때 돈이 사라지지 않는지.

토큰 없이 돌아갑니다. 디스코드에 연결하지 않고 코그 등록까지만 해봐요.
슬래시 명령 동기화 규격과 설명문 길이도 미리 검사하므로, 선택지가 0개라거나 설명이
너무 길어서 동기화가 통째로 실패하는 사고를 여기서 잡을 수 있습니다.

`--max-names`는 따로 칠 필요가 없어요. 기본 실행이 끝나면 **자동으로 한 번 더** 돕니다.
(이름은 import 시점에 설명문으로 굳어서, 한 프로세스에서 두 번 볼 수가 없어요)

콘솔이 cp949라 이모지에서 터지면 `PYTHONIOENCODING=utf-8`을 앞에 붙이세요.
(`main.py`는 스스로 UTF-8로 바꾸니 그냥 켜도 안 깨져요. 검사 도구만 해당됩니다)

### 🚨 검사 도구만 믿지 말고 한 번은 띄워볼 것

도구가 보는 건 **명령**과, 2026-08-16부터는 **도움말·설명문의 낱말**이에요.
여전히 못 보는 게 있습니다 — **명령을 실행했을 때 나오는 임베드 속 글자**요.
도구는 명령을 등록만 해보고 실행하지는 않거든요. 실제로 이렇게 놓쳤어요 —

> 상점을 안 담은 서버인데 도움말 카테고리 제목이 "지갑 및 **상점**"으로 남아 있었고,
> `/지갑내역 조회` 설명이 "(송금·출석·**상점·주식** 전부)"라고 없는 기능을 나열했습니다.
> 줄은 전부 제대로 걸러졌는데 제목만 고정 문자열이었어요. 검사는 전부 통과했습니다.
> (이 두 가지는 이제 `check_help.py`가 잡습니다)

> 그 뒤 2026-08-16에 **임베드 쪽**에서 여섯 군데가 더 나왔어요. `/지갑 전체`가 주식을
> 안 담은 서버에 "주식 시스템을 불러올 수 없어요"라고 띄우고 있었고, `/설정 채널지정내역`은
> 주문하지 않은 채널 열두 칸을 "❌ 미설정"으로 나열했습니다. 전부 명령을 **실행해야만**
> 보이는 글자라, 구성별로 화면을 찍어보고서야 찾았어요. 위 '기능 누출' 절 참고.

토큰이 없는 PC에서도 **로그인 직전까지는** 진짜로 켜볼 수 있어요.

```bash
cd mari && python main.py     # 설정 경고 → "봇 토큰이 없어요!" 까지 갑니다
```

여기까지 오면 설정 읽기·데이터 파일 생성·경고 출력이 실제로 도는 걸 본 거예요.
다만 코그 등록(`setup_hook`)은 로그인 **뒤에** 일어나서 안 돌아요. 코그까지 보려면
`tools/check_modules.py`가 같은 경로를 밟습니다.

납품 구성별로 유저가 실제로 볼 화면이 궁금하면, 임시 `guild.json`을 만들고
`MARI_GUILD_CONFIG`·`MARI_DATA_DIR` 환경변수로 가리킨 뒤 `_user_categories()`를
찍어보세요. **진짜 `mari/data/`는 절대 건드리지 마세요.**

### 창고에 넣는 방식

`mari/parked/README.md` 참고. 요약하면:

- 파일 통째로 옮길 수 있으면 `.py` 그대로 (그 자체로 온전한 모듈)
- 여러 파일에서 오려낸 조각 모음이면 `.py.txt`
  (실행되지 않으므로, 파이썬이 실수로 import하거나 문법 검사에 걸리지 않게)
- 블록마다 **원래 어느 파일 어디에 있었는지** 제목으로 남길 것
- 기능이 쓰던 **데이터 파일 상수도 같이** 걷어낼 것. 남겨두면 아무도 안 쓰는 빈
  JSON이 배포마다 생기고 매일 백업됩니다

### 밟았던 함정 (다시 밟지 말 것)

- **`_FILE`로 끝나는 전역 이름을 새로 만들지 말 것.** `json_data_files()`가
  "`_FILE`로 끝나고 `.json`인 전역"을 전부 데이터 파일로 보고 자동 생성·자동
  백업합니다. 설정 파일이 여기 걸리면 빈 `{}`로 조용히 덮어써져요.
  그래서 `GUILD_CONFIG_PATH`는 `_PATH`입니다.
- **`migrate_legacy_data_files()`가 코드 옆 `.json`을 전부 `data/`로 옮깁니다.**
  설정 파일은 `_CONFIG_FILENAMES`로 제외해뒀어요. 새 설정 파일을 코드 옆에
  둔다면 거기 추가하세요. (한 번 당했습니다)
- **`app_commands.choices()`에 빈 리스트를 넘기지 말 것.** 단, 여기 적혀 있던
  "동기화 전체가 실패한다"는 **사실이 아니었습니다.** 2026-08-15에 discord.py 2.7.1로
  직접 확인했어요 — 데코레이터도 통과하고, payload도 정상 생성되고, `choices` 키만
  조용히 빠집니다. 즉 **동기화는 멀쩡히 성공**하고 드롭다운이던 칸이 **자유 입력
  칸으로 바뀝니다.**

  ```
  @app_commands.choices(기능=[])  →  payload: {"type": 3, "name": "기능", ...}
                                      (choices 키 자체가 없음, 예외 없음)
  ```

  터지지 않으니 더 고약해요. 관리자가 아무 글자나 넣을 수 있고 뭘 넣든 매칭이 안 돼서
  아무 일도 안 일어납니다. **검사 도구도 못 잡습니다** — 원래부터 자유 입력이던 칸과
  구분할 방법이 없어요. 그러니 선택지를 설정에서 만들어 넣는 명령은 **만드는 쪽에서
  "비면 명령을 통째로 뺀다"** 로 처리해야 합니다.
  (`cogs/setting.py`의 `_prune_module_commands`가 `/기능제어`에 그렇게 하고 있어요.
  같은 파일의 `_feature_choices`는 이 잘못된 전제로 만든 거라 실효가 없습니다)

  ⚠️ **`discord.ui.Select`는 이야기가 다릅니다.** 그쪽은 선택지가 0개면 디스코드가
  진짜로 거부해요. `cogs/help.py`가 빈 드롭다운을 피하는 건 옳은 처리입니다.
  둘을 같은 것으로 묶어 생각하다가 이 항목이 틀리게 적혔던 것 같아요.
- **명령 설명문이 100자를 넘어도 똑같이 동기화 전체가 실패합니다.** 위 '이름 길이
  제한' 절 참고. `check_modules.py`가 자동으로 잡아줘요.
- **하위 명령이 0개인 그룹도 등록이 거부됩니다.** 기능을 빼다가 `/설정 채널`이
  통째로 비면 봇의 명령이 전부 사라져요. 비게 된 그룹은 그룹째로 빼야 합니다.
  (`cogs/setting.py`의 `_prune_module_commands` 참고)
- **명령을 걷어낼 거면 `cog_load`가 아니라 `__init__`에서 하세요.** `cog_load`는 트리
  등록보다 **나중에** 불려서, 최상위 그룹(`/기능제어`)을 빼려 해도 이미 늦습니다.
- **`remove_command()`는 지운 명령을 돌려주지 않아요(항상 `None`).** 반환값으로
  "지웠는지"를 판단하면 로그에 아무것도 안 남습니다. 있는지 먼저 보고 지우세요.
- **🚨 명단을 칸으로 나눌 때 "아무 칸에도 안 걸리는 사람"을 반드시 받아내세요.**
  레벨 역할표를 걷어낸 뒤 실제로 명단이 텅 빈 적이 있고, 회사 PC의 `ranks` 구현도
  같은 함정을 밟았어요(등급 역할이 없는 사람을 그냥 버림). 지금은 마지막 '미분류'
  칸이 등급 설정과 무관하게 항상 있습니다. `check_guild.py`가 이걸 지켜요.
- **없어진 명령을 설명문에서 백틱으로 감싸지 말 것.** 이 코드베이스에서 백틱은
  "지금 칠 수 있는 명령"이라는 뜻이라, 유저가 쳐볼 수 있는 걸로 오해합니다.
  `check_help.py`도 안내 중인 명령으로 잡아요. (`/랭킹`이 실제로 걸렸습니다)
- **🚨 도움말에 줄을 추가하면 모듈 태그를 꼭 다세요.** `cogs/help.py`의 각 줄은
  `(모듈, "문구")` 꼴이에요. 태그를 빠뜨리면(`None`) 그 기능을 안 담은 서버에서도
  안내돼서, 유저가 없는 명령을 보게 됩니다. `check_help.py`가 조합별로 잡아줘요.
- **🚨 카테고리 이름과 설명 문구에도 기능 이름을 박지 말 것.** 줄만 걸러내면 제목은
  그대로 남아요. 상점을 안 담은 서버에 "지갑 및 **상점**"이라는 제목만 덩그러니
  남은 적이 있습니다. 이름도 `module_active()`로 고르세요.
  (2026-08-16부터 `check_help.py`의 낱말 검사가 이 부류를 잡아줍니다. 다만 낱말 표에
   적힌 것만 봐요 — 위 '기능 누출' 절 참고)
- **🚨 임베드에 칸을 붙일 때 "그 기능을 안 담았으면 칸째로 뺄 것".** 0으로라도 채우면
  주문하지 않은 기능이 광고되고, "불러올 수 없어요" 같은 안내까지 붙으면 멀쩡한 봇이
  고장 난 것처럼 보입니다. `/지갑 전체`가 실제로 그랬어요.
  **`get_cog()`가 None인 것과 "안 담았다"는 다릅니다.** 전자는 사고(알려야 함),
  후자는 정상(조용히 빼야 함)이에요. 후자는 `module_active()`로 봅니다.
- **🚨 안 담은 기능의 설정 칸을 "❌ 미설정"으로 보여주지 말 것.** `/설정 채널지정내역`이
  주문하지 않은 채널·역할을 열 줄씩 나열하고 있었어요. 설정 **명령**은 이미
  `_prune_module_commands`가 걷어내니, 그 결과를 **보여주는 창**도 같은 기준
  (`is_active("channels"/"roles", ...)`)으로 걸러야 짝이 맞습니다.
- **명령 설명문에는 "담긴 기능의 …"처럼 쓰세요.** 설명은 데코레이터라 import 시점에
  굳어서 모듈 구성을 볼 수 없어요. 나열해두면 그 서버 명령 목록에 그대로 뜹니다.
- **설명에 "송금·출석·상점·주식 전부" 같은 나열을 넣지 말 것.** 담지 않은 기능까지
  적히고, 기능이 늘 때마다 고쳐야 해요. 어차피 전부 보여준다면 그냥 "전부"라고 쓰세요.
- **🚨 이름을 일괄 치환할 땐 조사(을/를·이/가)를 같이 고칠 것.** "마리"(받침 없음)를
  "봇"(받침 있음)으로 바꾸면 "마리가"가 **"봇가"**가 됩니다. 실제로 주석을 정리하다
  76줄을 그렇게 망가뜨려서 되돌렸어요. 코드가 `josa()`로 관리하는 바로 그 문제인데,
  주석에는 그 장치가 없으니 치환 스크립트가 직접 처리해야 합니다.
  받침 없음→있음: 가→이, 를→을, 는→은, 와→과, 로→으로.
- **일괄 치환 뒤에는 반드시 눈으로 읽을 것.** 조사 말고도 "에바시 이벤트"가
  "이벤트 이벤트"가 되고, 실제 명령 이름이던 "에바 보내기"가 "재화 보내기"라는
  없는 명령이 됐어요. 기계적으로 맞아도 문장이 틀릴 수 있습니다.
- **🧩 새 채널·역할·데이터 파일을 만들면 `modules.py`에 소유 모듈을 등록하세요.**
  안 하면 그 키는 '공용'으로 취급돼 **항상 켜진 채로 남습니다.** (빠뜨렸을 때 기능이
  사라지는 것보다 안 지워지는 게 덜 위험해서 일부러 그런 기본값이에요) 조용히
  어긋나는 게 문제라, `check_modules.py`의 '소유 표' 검사가 대조해줍니다.
- **선택지가 0개인 드롭다운(`discord.ui.Select`)도 명령을 죽입니다.** 코어 모듈만
  담으면 유저용 도움말 카테고리가 통째로 비어요. `help_command`가 그때는 드롭다운
  없이 안내만 보내도록 막아뒀습니다. 카테고리를 동적으로 만들 땐 항상 이걸 보세요.
- **`mari_names`는 `mari_settings`를 씁니다.** 그래서 `mari_settings` 쪽에서 이름이
  필요하면 파일 맨 위가 아니라 **함수 안에서** import해야 해요. 서로를 부르는
  순환 import가 되면 봇이 기동조차 못 합니다. (`build_log_embed` 참고)
- **일괄 수정 스크립트로 코드를 오려낼 때**, 잘라낸 내용을 창고 파일에 **먼저**
  쓰고 원본을 고치세요. 검증에 걸려 중간에 멈추면 오려낸 코드가 사라집니다.
  (한 번 당해서 깃에서 복구했습니다)
- **파이썬 3.12부터 f-string은 토큰 하나가 아닙니다.** `tokenize`로 문자열을 훑는
  스크립트를 짤 때 `STRING`만 보면 f-string을 통째로 놓쳐요. `FSTRING_MIDDLE`을
  같이 봐야 합니다. (이름 변수화 1차 스크립트가 여기 걸려서 절반을 놓쳤어요)
- **기능을 뺄 때 그 기능을 참조하는 다른 파일을 반드시 훑을 것.** `/고확`을 지우려다
  `birthday.py`가 같은 역할 상수를 쓰고 있는 걸 뒤늦게 발견했어요. 그냥 지웠으면
  봇이 기동조차 못 했습니다. 뺀 뒤에는 `grep`으로 이름을 한 번 더 훑고
  `check_modules.py`를 돌리세요.
- **소스에 `\u001b` 같은 이스케이프를 넣을 때 편집 도구를 믿지 말 것.** 실제 제어문자로
  바뀌어 박히는 일이 있었어요. 스크립트로 `chr(92)`를 써서 백슬래시를 직접 만들고,
  쓴 뒤에 `"\u001b" not in text` 로 검증하세요.

### 로컬에만 있는 것

`mari/guild.json`(실제 역할 ID가 든 로컬 설정)과 `mari/data/`, 그리고 `.claude/`는
`.gitignore` 대상이라 **집 PC에는 없습니다.** 원본 서버 ID가 필요하면
`mari/guild.example.json`을 복사해서 채우거나, 회사 PC의 파일을 옮기세요.
값 자체는 `maribot` 레포의 깃 히스토리에도 남아 있습니다.

없어도 `tools/check_modules.py`는 잘 돌아갑니다. (역할 ID가 비었다고 경고만 해요)
