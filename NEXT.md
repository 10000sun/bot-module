# 다음에 할 일

*2026-08-14 기준. 서버 고유 기능 제거(1~4번)를 끝낸 상태입니다.*

## 지금 어디까지 왔나

`maribot` 코드를 가져와 **모듈로 골라 담을 수 있는 형태**로 바꾸는 중입니다.

- ✅ 코드만 복사 (유저 데이터·깃 히스토리는 안 가져옴)
- ✅ 서버 고유 ID를 코드에서 걷어내 `mari/guild.json` 으로 분리
- ✅ 모듈 레지스트리(`mari/modules.py`) + 동적 로딩. 담을 기능은 `guild.json`의 `modules`가 결정
- ✅ 코그 간 직접 import 제거 (이제 14개 코그가 전부 독립)
- ✅ 서버 고유 기능 일부를 `mari/parked/` 창고로 (캠프·명단·프로필·견학권)
- ✅ **남은 서버 고유 기능 제거** — 고확·유배·타운가이드·페르소나, 등급 체계 설정화
- ⬜ **이름 변수화 + 첫 기동 입력받기** ← 여기부터
- ⬜ **도움말을 모듈에 맞춰 보여주기** (아래 1번, 새로 발견)

현재 상태: 모듈 14개, 슬래시 명령 33개.
`python tools/check_modules.py` 와 `python tools/check_help.py` 로 확인 가능.

---

## 0. 이번에 무엇을 했나 (참고용)

창고로 보낸 것 — 전부 `mari/parked/` 에 원래 자리를 적어 보관했습니다.

| 파일 | 무엇 |
|---|---|
| `games_broadcast.py.txt` | 고확(고성능 확성기) |
| `setting_exile.py.txt` | 유배·복귀(사면), 추방관·지옥간수·유배자 |
| `setting_onboarding.py.txt` | 타운가이드, 입주 가이드 프리셋 |
| `gpt_persona.py.txt` | AI 페르소나 분기, `check_my_profile` 도구 |

**대신 생긴 범용 설정**

- `roles.announce_mention` — 생일 축하 등 전체 알림에 멘션할 역할
  (`broadcast_mention`을 대체. 원본 서버에서는 고확 멘션 역할과 시민권 역할이 같은 값이었어요)
- `/설정 관리자 역할부여` (`role_admin`) — `/역할부여`를 쓸 수 있는 역할
  (타운가이드 역할 ID가 코드에 박혀 있던 자리)
- `/설정 관리자 대장` — 원래 `/설정 레벨 대장`이었어요. `chief_role`이 하던 두 가지 일
  (아이디 명단의 '대장' 칸 + `/기능제어` 권한) 중 명단 쪽을 아래 `ranks`가 가져가서,
  이제 `/기능제어` 권한 전용 역할입니다. `설정 레벨` 그룹은 비어서 없앴어요.
- `ranks` — 등급 사다리 (아래 참고)

**등급 체계는 (b) 설정으로 빼는 쪽으로 정했습니다.**
`guild.json`의 `ranks`에 `{label, role, color}`를 순서대로 적으면 아이디 명단을 그 순서대로
묶어서 게시해요. **안 적으면 등급 구분 없이 한 덩어리로** 게시합니다(아이디를 등록한
사람만). 형식과 쓸 수 있는 색은 `guild.example.json`의 `_ranks설명`에 적어뒀어요.

`/아이디 가져오기`의 문서 파서도 이 `label`을 구획 제목으로 찾도록 바꿨습니다.
(예전엔 '대장 / N레벨 / N LEVEL'이 정규식에 박혀 있었어요)

**그 밖에 정리한 것**

- 창고로 간 기능의 데이터 파일 상수 5개(`CAMP_*`·`PROFILE_FILE`·`VISIT_PASS_ASK_FILE`)와
  `PROFILE_PHOTO_DIR`를 걷어냈어요. 안 쓰는데 `json_data_files()`가 **매번 빈 파일을 만들고
  매일 백업**하고 있었습니다. 원래 줄은 `camp_leftovers.py.txt`·`shop_visit_pass.py.txt`에.
- 명단 문서 파서가 ESC 문자를 안 지워서, 메세지를 파일로 내려받아 올리면 구획 제목도
  사람 이름도 하나도 못 잡던 버그를 고쳤습니다. (화면에서 복사한 것만 동작했어요)
- `/설정 관리자 프로필`을 없앴어요. 프로필 모듈이 창고로 간 뒤에도 명령만 살아남아서,
  역할을 지정해도 그걸 볼 코그가 없어 아무 일도 안 일어났습니다.
  (`camp_leftovers.py.txt`에 보관)
- `tools/check_help.py` 추가 — 아래 참고.

---

## 1. 도움말이 모듈을 안 봐요 🔴 (새로 발견)

`cogs/help.py`의 안내문은 **어떤 모듈이 올라왔는지 보지 않는 고정 문자열**입니다.
그래서 상점만 담아 납품해도 `/도움말`이 주식·아이디·생일 명령을 그대로 안내해요.

`tools/check_help.py`를 모듈 조합별로 돌려보면 바로 보입니다. (그래서 그 도구는
일부러 "모듈 전부 켠 상태"로만 검사하게 해뒀어요. 섞어 보고하면 진짜 오타를 못 찾아서요)

생각해볼 방법:

- 카테고리마다 "이 모듈이 있어야 보임" 표시를 달고, `bot.loaded_modules`로 걸러내기
  (`modules.py`의 키를 그대로 쓰면 됩니다)
- 명령 안내를 각 코그가 들고 있게 하고 도움말은 모아서 보여주기 (더 크지만 더 깔끔)

첫 번째가 작고 확실해 보입니다.

## 2. 이름 변수화 + 첫 기동 입력받기

**정한 방향:** 이름을 코드에 박지 말고 변수로 두고, 봇을 처음 초대해서 켰을 때
입력을 받는다.

바꿔야 할 이름:

| 지금 | 무엇 | 나오는 파일 수 |
|---|---|---|
| 에바 | 서버 재화 이름 | 16개 |
| 마리 | 봇 이름 | 17개 |
| 에바시 | 선착순 이벤트 이름 | `games.py`, `setting.py`, `diagnostics.py` |
| 에바스타운 | 서버 이름 | `shop.py` 등 |

⚠️ 아이디 명단 제목에 박혀 있던 "에바스타운"은 이번에 `guild.name`(디스코드 서버 이름)을
쓰도록 이미 고쳤어요. 같은 방법이 통하는 자리가 더 있는지 먼저 보세요. 설정을 하나도
안 늘리고 해결되는 게 제일 좋습니다.

**입력받는 방법은 아직 안 정했습니다.** 콘솔 입력은 위험해 보여요 — 이 봇은
systemd/도커로 돌리는 걸 전제로 짜여 있어서(`main.py` 주석 참고) 표준입력이
없을 수 있습니다. 후보:

- `/초기설정` 슬래시 명령을 관리자가 실행 → 값을 받아 저장 (가장 안전해 보임)
- 봇이 서버에 처음 들어왔을 때(`on_guild_join`) 서버 주인에게 DM으로 물어보기
- 위 둘을 같이 (자동으로 말 걸고, 놓치면 명령으로 다시)

저장 위치도 정해야 합니다. `guild.json`(배포 설정)과 `settings.json`(런타임 설정)
중 어디가 맞는지 — 첫 기동에 봇이 스스로 쓰는 값이면 `settings.json` 쪽이
자연스러워 보입니다.

## 3. 그 밖에 남은 것

- **`cogs/core.py` 파일 이름.** '핵심'처럼 보이지만 실제로는 게임 아이디 등록부예요.
  (`modules.py`의 `id` 모듈 설명에도 적혀 있습니다)
- **`/연대기`는 도움말에 없어요.** 원본 서버 운영 방침이라 일부러 뺀 건데
  (`help.py` 주석 참고), 납품할 때는 담았으면 안내도 하는 게 맞습니다.
  연대기를 담아 파는 순간 카테고리를 하나 추가하세요.

---

## 작업 요령

### 확인하는 법

```bash
python -m venv .venv
.venv\Scripts\python -m pip install -r mari/requirements.txt

.venv\Scripts\python tools/check_modules.py                # 지금 설정 그대로
.venv\Scripts\python tools/check_modules.py shop birthday  # 이 조합만 주문받았다면
.venv\Scripts\python tools/check_modules.py --none         # 설정 없는 새 서버처럼

.venv\Scripts\python tools/check_help.py                   # 도움말 ↔ 실제 명령 대조
```

토큰 없이 돌아갑니다. 디스코드에 연결하지 않고 코그 등록까지만 해봐요.
슬래시 명령 동기화 규격도 미리 검사하므로, 선택지가 0개라 동기화가 통째로
실패하는 사고를 여기서 잡을 수 있습니다.

`check_help.py`는 **없는 명령을 안내하고 있으면 종료 코드 1**로 끝나요. 기능을 창고로
보낼 때마다 `help.py` 고치는 걸 계속 빠뜨려서(실제로 한 커밋이 그대로 지나갔어요)
자동화해뒀습니다. 기능을 떼어냈으면 이걸 꼭 돌려보세요.

### 창고에 넣는 방식

`mari/parked/README.md` 참고. 요약하면:

- 파일 통째로 옮길 수 있으면 `.py` 그대로 (그 자체로 온전한 모듈)
- 여러 파일에서 오려낸 조각 모음이면 `.py.txt`
  (실행되지 않으므로, 파이썬이 실수로 import하거나 문법 검사에 걸리지 않게)
- 블록마다 **원래 어느 파일 어디에 있었는지** 제목으로 남길 것
- 그 기능이 빠지면서 **같이 죽는 import·상수**도 창고 파일에 적어둘 것
  (안 적으면 되살릴 때 이름을 못 찾아서 하나씩 더듬어야 해요)

### 밟았던 함정 (다시 밟지 말 것)

- **`_FILE`로 끝나는 전역 이름을 새로 만들지 말 것.** `json_data_files()`가
  "`_FILE`로 끝나고 `.json`인 전역"을 전부 데이터 파일로 보고 자동 생성·자동
  백업합니다. 설정 파일이 여기 걸리면 빈 `{}`로 조용히 덮어써져요.
  그래서 `GUILD_CONFIG_PATH`는 `_PATH`입니다.
  반대로, **기능을 떼어낼 때 이 상수를 안 지우면** 그 기능이 없는 서버에서도
  빈 파일이 계속 생기고 백업됩니다. (이번에 5개를 치웠어요)
- **`migrate_legacy_data_files()`가 코드 옆 `.json`을 전부 `data/`로 옮깁니다.**
  설정 파일은 `_CONFIG_FILENAMES`로 제외해뒀어요. 새 설정 파일을 코드 옆에
  둔다면 거기 추가하세요. (한 번 당했습니다)
- **`app_commands.choices()`에 빈 리스트를 넘기지 말 것.** 디스코드가 명령어
  등록을 거부해서 **동기화 전체가 실패**합니다. 설정이 비면 선택지를 아예
  안 다는 식으로 처리해뒀어요 (`parked/roster.py`의 `_with_dynamic_choices` 참고).
- **명령이 하나도 없는 `app_commands.Group`을 남기지 말 것.** 같은 이유로 위험합니다.
  `설정 레벨` 그룹이 이번에 이렇게 비어서 통째로 없앴어요.
- **일괄 수정 스크립트로 코드를 오려낼 때**, 잘라낸 내용을 창고 파일에 **먼저**
  쓰고 원본을 고치세요. 검증에 걸려 중간에 멈추면 오려낸 코드가 사라집니다.
  (한 번 당해서 깃에서 복구했습니다)
- **소스에 ESC 문자를 그대로 넣지 말 것.** ANSI 색을 다루는 코드가 몇 군데 있는데,
  `\u001b`·`\x1b` 같은 **escape 표기**로 적으세요. 진짜 ESC 바이트를 넣으면 눈에
  안 보여서 나중에 고칠 때 문자열이 안 맞습니다.

### 로컬에만 있는 것

`mari/guild.json`(실제 역할 ID가 든 로컬 설정)과 `mari/data/`는 `.gitignore`
대상이라 **집 PC에는 없습니다.** 원본 서버 ID가 필요하면
`mari/guild.example.json`을 복사해서 채우거나, 회사 PC의 파일을 옮기세요.
값 자체는 `maribot` 레포의 깃 히스토리에도 남아 있습니다.

⚠️ 원본 서버의 **레벨 역할 ID(0~4레벨)** 는 이 레포 어디에도 없어요. 이미 지난
커밋에서 `LEVEL_ROLES`째로 걷어냈습니다. 로컬 `guild.json`에 `ranks`를 채워 시험해
보려면 `maribot` 깃 히스토리에서 꺼내오셔야 해요. (안 채우면 한 덩어리로 게시됩니다)

없어도 `tools/check_modules.py`는 잘 돌아갑니다. (역할 ID가 비었다고 경고만 해요)
