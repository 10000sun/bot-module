# -*- coding: utf-8 -*-
# 마리봇 실행에 필요한 패키지
#
# 설치:  pip install -r requirements.txt
#
# ⚠️ 맨 위 coding 줄과 파일 앞머리의 BOM은 지우지 마세요. 한국어 윈도우에서 pip은
#    BOM도 coding 줄도 없으면 이 파일을 cp949로 읽으려다 한글에서 그대로 죽습니다.
#    (UnicodeDecodeError: 'cp949' codec can't decode byte 0x87)
#
# 버전은 일부러 고정하지 않았어요. 서버마다 파이썬 버전이 달라서 못 박아두면
# 오히려 설치가 막히는 경우가 있습니다. 특정 버전으로 굳혀야 할 일이 생기면
# 그때 `pip freeze > requirements.lock.txt`로 따로 잠가두세요.
#
# 파이썬 3.10 이상이 필요합니다. (2026-08 기준 3.12에서 동작 확인)

# 디스코드 봇 본체. 슬래시 명령어·버튼·드롭다운 전부 이 패키지예요.
discord.py>=2.4

# 마리 AI 대화 (cogs/gpt.py). GEMINI_API_KEY가 없으면 대화 기능만 꺼지고
# 나머지는 정상 동작하지만, import는 되어야 해서 설치는 필요합니다.
google-genai

# 주가 추이 그래프 이미지 생성 (cogs/stock.py)
matplotlib

# 주식 개폐장 판정에 쓰는 한국 공휴일 정보 (cogs/stock.py)
holidays

# ─────────────────────────────────────────────────────────────
# 별도 설치가 필요 없는 것들 (표준 라이브러리)
#   .env 읽기      → python-dotenv 대신 chunsik_config.load_env_file()을 직접 씁니다
#   시간대(KST/PT) → zoneinfo (3.9+)
#   웹훅 알림      → urllib (chunsik_alerts.py)
# ─────────────────────────────────────────────────────────────
